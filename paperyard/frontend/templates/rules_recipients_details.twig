{% extends 'layout.twig' %}

{% block content %}

<div class="container-fluid">

    <div class="animated fadeIn">

        <div class="row">

            <div class="col-lg-12">

                <div class="card">
                    <form action="/rules/recipients/{{ recipient.id }}/save" method="post">
                        <div class="card-header">
                            {% trans "Rule Details" %}
                        </div>
                        <div class="card-body">
                            <div class="form-group">
                                <label for="recipientNameInput"><strong>{% trans "Long Name" %}</strong></label>
                                <input type="text" class="form-control" name="recipientNameInput" id="recipientNameInput" value="{{ recipient.recipientName }}">
                            </div>
                            <div class="form-group">
                                <label for="shortNameForFileInput"><strong>{% trans "Name For File" %}</strong></label>
                                <input type="text" class="form-control" name="shortNameForFileInput" id="shortNameForFileInput" value="{{ recipient.shortNameForFile }}">
                            </div>
                            <div class="form-group">
                                <label for="activeInput"><strong>{% trans "Status" %}</strong></label><br>
                                <label class="switch switch-xl switch-default switch-info">
                                    <input type="checkbox" class="switch-input" name="activeInput" id="activeInput"{% if recipient.isActive == 1 %} checked{% endif %}>
                                    <span class="switch-label"></span>
                                    <span class="switch-handle"></span>
                                </label>
                            </div>
                        </div>
                        <div class="card-footer">
                            <button type="submit" class="btn btn-md btn-success">{% trans "save" %}</button>
                            <div class="pull-right">
                                <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
                                    {% trans "delete" %}
                                </button>
                            </div>
                        </div>
                    </form>
                </div>

            </div>

        </div><!-- /.row -->
    </div>

</div>

{% include 'rule_delete_modal.twig' %}

{% endblock %}